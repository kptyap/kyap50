#include <stdio.h>
#include <cs50.h>

int main(void) {
    
    printf("Height:");
    int h = GetInt();
    
    if (h >0 && h <=23)
    {
        for (int i = 0; i < h, i++)    
        {printf("%*s", h-i);//print spaces to formula spaces = h-i
        
        {printf("%s", h+1, #);//print hashes to formula hashes = i + 1
        
        {printf("\n");//print new line

    }
    else
    {
    printf("Height:");
    int height = GetInt();
    }